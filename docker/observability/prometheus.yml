global:
  scrape_interval: 10s

scrape_configs:
  - job_name: eventops
    metrics_path: /actuator/prometheus
    basic_auth:
      username: prometheus
      password_file: /run/secrets/observability_password
    static_configs:
      - targets: ["aplicacao:9090"]
