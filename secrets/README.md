# Segredos locais

Copie `observability_password.example.txt` para `observability_password.txt` e troque o conteudo antes de iniciar os containers. Arquivos `*.txt` desta pasta sao ignorados pelo Git.

Em producao, monte o valor a partir do gerenciador de segredos da plataforma. Nao reutilize a senha local.
